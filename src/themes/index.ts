import mayk from "./mayk";
import khin from "./khin";
import chaya from "./chaya";
import mint from "./mint";

export const themes = {
  mayk,
  khin,
  chaya,
  mint,
};

export const brands = {
  mayk: mayk.page.brand,
  khin: khin.page.brand,
  chaya: chaya.page.brand,
  mint: mint.page.brand,
};
